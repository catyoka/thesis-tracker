# Set to "postgresql" to use PostgreSQL.
# Any other value (or unset) falls back to SQLite.
DB_ENGINE=postgresql

DB_NAME=betteranilist
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=127.0.0.1
DB_PORT=5432
